<h1>Create a Heroine</h1>
<%= form_for @heroine do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :super_name %>
<%= f.text_field :super_name %>
<%= f.collection_select :power_id, Power.all, :id, :name %>

<%= f.submit %>
<% end %>


<!-- 

5. As a visitor to the website, I should be able to create a new 
heroine with her name and super name. -->


<!-- 6. The form should also allow each heroine to be created with
     **only one of the existing powers**. -->
